#!/usr/bin/python
import os
def shred(filename, iterations, zero, remove):
	#shredding function
	command = "shred " + "-n" + iterations
	if(zero == "y"):
		command += " -z"
	if(remove == "y"):
		command += " -u"
	command += " " + filename
	os.system(command)

